#include <iostream>
#include <vector>
#include <map>
using namespace std;

#define maxVariableNum 25

main()
{
    ios::sync_with_stdio(false);

    vector<char>variable;
    variable.reserve(maxVariableNum);

    bool firstCase = true;

    int caseNum;
    cin >> caseNum;
    
    while(caseNum--) {
        variable.clear();

        if (firstCase)
            firstCase = false;
        else 
            varible.push_back(tmp);

        while(cin >> tmp && tmp.size()==1)
            variable.push_back(tmp);

        tmp[0] tmp[2]
        while(cin >> tmp && tmp.size()==3) {

        }
    }
}
