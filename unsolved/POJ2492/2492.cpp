#include <cstdio>

main()
{
    int caseNum, bugNum, interactionNum;
    int bug1, bug2;
    scanf("%d", &caseNum);
    while(caseNum--) {
        scanf("%d%d", &bugNum, &interatcionNum);

        bool isNormal = true;
        while(interactionNum--) {
            scanf("%d%d", &bug1, &bug2);
            if (isNormal) {

            }

        }

        
    }

    printf("Scenario")
}
